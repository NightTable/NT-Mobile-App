// import { RootReducerState, RootAction } from "types";
// import { RootActionType } from "../actions/root";

const initialState = {
  Auth: [],
};

const rootReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default rootReducer;
